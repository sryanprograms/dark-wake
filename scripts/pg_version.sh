#!/usr/bin/env bash
# Homebrew PostgreSQL version with PostGIS extension files bundled by brew.
# postgis formula ships extensions for @17 and @18 only (not @16).
export DARKWAKE_PG_VERSION="${DARKWAKE_PG_VERSION:-17}"
